'use client'

/**
 * ChangelogOverlay — disabled for end users.
 * Previously auto-showed developer-facing technical details.
 * Kept as a no-op so layout.tsx import doesn't break.
 */
export default function ChangelogOverlay() {
  return null
}
